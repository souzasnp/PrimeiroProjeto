* Linguagem de Programação (Python)

Anotações:

- Python é uma Linguagem de propósito geral.
- Multiplataforma.
- battarias included.
- Open Source
- Linguagem orientada a Objetos.
- Muitas bibliotecas.

Instalando Python
   - python.org
   - add patch
   - disable final da instalação

         Python
obs: todos os comandos são funções e  toda e qualquer função no python3 exige parenteses.

obs: toda variavel em python é um objeto.

-Primeiros Comandos em Python
      - print ('Comando que escreve na tela')
      -   =   ('Utilizando quando alguma variavel recebe algo)
      - input ('Comando Leia tem a função de interagir com o usuario)

- Tipos Primitivos
      - int ('Números Inteiros')
      - float ('Números de Ponto flutuante ou número real')
      - bool ('boleanos True ou False')
      - str ('Caracteres 'Olá', '7.5')
      
Aula 07 - curso em video
- Operadores Aritiméticos
      - (+) Adição
      - (-) Subtração
      - (*) Multiplicação
      - (/) Divisão
      - (**)Potência
      - (//)Divisão Inteira
      - (%) Resto da Divisão
   * Ordem de Procedência
      1 - ()
      2 - **
      3 - * / // %
      4 - + - 

FASE 08 - TRABALHANDO COM MODULOS
      - import (Quando utilizado sozinho importa toda a biblioteca)
ex: import (biblioteca)
      - from (importa somente o que for especificado) 
ex: from (biblioteca) import (item)
      - math (importa funções matematicas)
      - ceil (arredonda para cima)
      - floor(arredonda para baixo)
      - trunc(Elimina o numero da virgula para frente)
      - pow  (potencia)
      - sqrt (Calcula Raiz quadrada)
      - Factorial ()

FASE 09 - MANIPULANDO TEXTO
      - Fatiamento (frase = [9]  ou [9:13] ou [9:21:2] ou [:5] ou [15:])
      - Analise (len(frase) / (frase.count('o', 0, 13) / (frase.find('deo)) /  )
      - Transformação (frase.replace('Python','Android') (frase.upper()) (frase.lower()) (frase.capitalize())
      (frase.title()) (frase.strip()) (frase.rstrip()) (frase.lstrip()))
      - Divisão (frase.split()) 
      - Junção ('-'.join(frase))

#Adicionando Cores
      - \033[style;text;back m]
      - Código Style: 0,1,4,7 (0 mantem o padrao, 1 negrio, 4 sublinhado, 7 negativa)
      - Código Text : (30 branco, 31 vermelho, 32 verde, 33 amarelo, 34 azul, 35 purple, 36 ciano, 37 cinza)
      - Código Back : (40 branco, 41 vermelho, 42 verde, 43 amarelo, 44 azul, 45 purple, 46 ciano, 47 cinza)